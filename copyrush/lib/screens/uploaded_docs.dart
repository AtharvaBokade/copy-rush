import 'package:flutter/material.dart';

class UploadedDocs extends StatelessWidget {
  const UploadedDocs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Uploaded documents"),
      ),
      body: Center(
        child: Text("Functionality not yet added"),
      ),
    );
  }
}
